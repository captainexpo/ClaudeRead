[project]
name = "clauderead"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "discord>=2.3.2",
]

[tools.pyright]
venvPath = "."
venv = ".venv"
exclude = [".venv"]
